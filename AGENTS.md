# Building with Terminal 3 ADK

Get authenticated SDK connectivity working before adding TEE contract code. Never commit `T3N_API_KEY`; tenant and agent identities require separate keys and balances. Use sandbox, a verified trust manifest, WASM crypto, handshake, and authentication. Always use the returned DID. Outbound capabilities require explicit data-owner delegation. Unknown behavior must fail closed.
